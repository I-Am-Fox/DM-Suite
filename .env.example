# Next.js-native auth
# APP_AUTH_SECRET must be at least 32 characters and kept server-only.
APP_AUTH_SECRET=
APP_AUTH_EMAIL=
APP_AUTH_PASSWORD=
APP_AUTH_USER_NAME=
APP_AUTH_USER_ID=usr_demo_dm

# Future application database.
# This will back campaigns, sessions, prep blocks, world entities, timeline events, and exports.
DATABASE_URL=
